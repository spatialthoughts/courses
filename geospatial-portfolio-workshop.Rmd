---
title: "Building Your Geospatial Portfolio Website"
subtitle: "A hands-on workshop for creating a modern portfolio website using GitHub Pages and MkDocs."
author: "Ujaval Gandhi"
fontsize: 12pt
output:
  html_document:
    df_print: paged
    toc: yes
    toc_depth: 2
    highlight: pygments
    includes:
      in_header: copy-code.html
      after_body: comment.html
      
  # word_document:
  #   toc: false
  #   fig_caption: false
  # pdf_document:
  #   latex_engine: xelatex
  #   toc: yes
  #   toc_depth: 3
header-includes:
- \usepackage{fancyhdr}
- \pagestyle{fancy}
- \renewcommand{\footrulewidth}{0.4pt}
- \fancyhead[LE,RO]{\thepage}
- \geometry{left=1in,top=0.75in,bottom=0.75in}
- \fancyfoot[CE,CO]{{\includegraphics[height=0.5cm]{images/cc-by-nc.png}} Ujaval Gandhi http://www.spatialthoughts.com}
classoption: a4paper
---

\newpage

***

```{r echo=FALSE, fig.align='center', out.width='75%', out.width='250pt'}
knitr::include_graphics('images/spatial_thoughts_logo.png')
```

***

\newpage

# Introduction 

This workshop is a gentle introduction to Git, GitHub and Web Development with the goal of creating and publishing a personal portfolio website. We will start with a ready-to-use portfolio website template for geospatial professionals and learn how to edit and set up your GitHub repository to publish your website. We will cover best practices on leveraging LLM-based AI assistants to extract and format content from existing documents and populate the portfolio.

At the end of the workshop, you will have a beautiful, modern and customized portfolio website that is hosted for free on GitHub Pages and ready to be shared with your network. 

**No prior experience with Git or GitHub is required.**

This workshop will cover the following topics:

- Introduction to GitHub and GitHub pages
- Introduction to Static Site Generators
- Fundamentals of Markdown
- Introduction to MkDocs
- Use of AI-Assistants to automatically extract and format content from your CV and Project Reports
- Setting up a local development environment and advanced customization

We will use the [MkDocs](https://www.mkdocs.org/) static site generator with [Material theme](https://squidfunk.github.io/mkdocs-material/) that creates modern responsive website from Markdown-formatted content.

[![View Portfolio](images/geospatial_portfolio_workshop/portfolio1.png){width="400px"}](https://spatialthoughts.github.io/){target="_blank"}

[View Example Portfolio Website &#8599; ](https://spatialthoughts.github.io/)


----

# Installation and Preparation

## Sign-up for GitHub

Visit [GitHub.com](https://github.com/) and create a free account. If you already have an account, skip this step.

## Install Git

### Windows

1. Go to [git-scm.com](https://git-scm.com/install/windows) and download the latest version ofr *Git for Windows*. During install, accept all defaults.


```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/gitwindows.png')
```

### Mac

Most versions of MacOS will already have Git installed, and you can activate it through the terminal. Open *Terminal* and run the following command. If Git is installed, it will print the version. If it is not installed, macOS will prompt you to install it.

```
git --version
```

```{r echo=FALSE, fig.align='center', out.width='60%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/gitmac.png')
```

### Linux

You can install Git via the package management tool for your Linux distribution. GitHub has a detailed [Install Git for Linux](https://github.com/git-guides/install-git#install-git-on-linux) guide with instructions for different operating systems.


## Configure Git

Once installed, *(Windows users)*, search for Command Prompt launch it. *(Mac/Linux users)*: Launch a Terminal window.

Run the following commands to set your name and email. Make sure to change `<Your Name>` and `<your-email@example.com>` with your own details.

```bash
git config --global user.name "<Your Name>"
```

```bash
git config --global user.email "<your-email@example.com>"
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/gitconfig.png')
```


## Install Conda

We will use conda to install the required Python packages and manage local development environment. Download the [Anaconda Installer](https://www.anaconda.com/download/success) for your operating system. Once downloaded, double click the installer and install it into the default suggested directory. 

## Get a Code Editor

If you already have a favorite text editor or an IDE (Integrated Development Environment), you may use it for this workshop. Otherwise, we recommend [Visual Studio Code (VS Code)](https://code.visualstudio.com/) - which can work with both text files and Jupyter Notebooks and has in-editor support for AI-Assisted coding.

> Tip: You will be prompted to activate AI-coding features by signing into your GitHub account. We recommend this but if you do not want it - you can skip it by closing the window.

## Prepare Content for the Portfolio

Before you start building the portfolio, it will be helpful to have the following items saved to a local directory on your computer.

* **Profile Picture**: Save your preferred profile photo as a PNG file named `profile.png`. Preferably 300px x 300px in size.
* **Homepage Picture**: A photo for the *About Me* section. Save a photo you would like to use on the homepage as a PNG file named `about.png`. Maximum 500px width.
* **Your Resume/CV**: Save a copy of your CV as a PDF file named `[YOUR NAME]-CV.pdf`. If you name is John, the file would be `John-CV.pdf`.
* **Project Artifacts**: Project Reports, Jupyter Notebooks or any other data relating to the projects you would like to showcase in the portfolio.


# 1. Introduction to GitHub Pages

[GitHub Pages](https://pages.github.com/) is a static site hosting service that turns your repository files into a live website.  The `gh-pages` branch is a special branch used by GitHub Pages. You can enable Github Pages on your repository and configure it publish the content from this branch to your user site.

## 1.1 Setting up a Repository

1. Login to your GitHub account. Go to the template repository at https://github.com/spatialthoughts/portfolio-website-template/. Go to **Use this template &rarr; Create a new repository**.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create1.png')
```

2. Under *General*, enter the `Repository name`. To use GitHub Pages for free at https://your-username.github.io, name the repository exactly as `your-username.github.io` (replace your-username with your actual GitHub username). For example, if the github username is `spatialthoughts`, the repository name should be `spatialthoughts.github.io`. This is a special repository naming pattern that GitHub uses to publish the site at the root of your GitHub pages . Optionally, you may use any other name and the the site will be published at https://your-username.github.io/your-repository-name. Leave all other options as-is and click *Create Repository*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create2.png')
```

3. Your repository will now be created. When you first copy the template, a GitHub Action will run and setup a new branch named `gh-pages` and enable Github Pages. Wait for a minute for these tasks to finish and then go to **Settings &rarr; Pages**. It will show the URL of the portfolio website. Click on it to open the website in a new tab. It is not yet setup with the correct content.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create3.png')
```

4. Scroll down to the *Branch* section and select `gh-pages` as the branch. Click *Save*. 

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create4.png')
```

5. GitHub will now trigger an Action to take the HTML files generated by MkDocs and publish it on the Github Pages site. Go to the *Actions* tab to monitor the progress and wait till the action is successfully completed.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create5.png')
```

6. Refresh the tab with your website. It should now show the default template website hosted on your GitHub Pages.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/create6.png')
```

# 2. Introduction to Markdown

Markdown is a simple formatting language to create structured documents. It allows you to write plain-text documents with simple markups that is both human and machine-readable. It is a widely used standard for creating documents, websites, and prompts for AI models. GitHub supports Markdown with some additional features in what is known as GitHub Flavored Markdown. We will now practice writing Markdown by editing the `README.md` file in your repository.

## 2.1 Updating the README.md

1. Go to your repository's homepage on GitHub and click on `README.md` file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown1.png')
```

2. Next, click the *Edit file* button.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown2.png')
```

3. Delete the existing text and enter new text about your repository in the Markdown format. Below is the sample text you can use. You can use the *Preview* tab to see the rendered version of your text.

> You can see Github's [Markdown Guide](https://docs.github.com/en/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax) for help with syntax.

```
# [YOUR NAME]'s Portfolio

This is the repository for my portfolio website. The portfolio is hosted at https://<your-username>.github.io/

 This website is built using the following tools
 
* MkDocs
* GitHub Pages
* GitHub Actions

> [!TIP]
> You can get the portfolio template from [Spatial Thoughts](https://github.com/spatialthoughts/portfolio-website-template)
 ```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown3.png')
```

 4. Check the preview and fix any formatting issues. Once you are done, click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown4.png')
```

 5. In the confirmation dialog, enter a *Commit message* and click *Commit changes*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown5.png')
```

 6. The `README.md` file acts as the primary documentation for your repository and is displayed on the homepage.

```{r echo=FALSE, fig.align='center', out.width='60%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/markdown6.png')
```

# 3. Introduction to MkDocs

[MkDocs](https://www.mkdocs.org/) is a Python-based static site generator focused on building project documentation sites. The template we are using is setup to use MkDocs to create the portfolio website using the following components. 

* **Content**: Each page is a Markdown (`.md`) file in the `docs/` folder.
* **Configuration**: All configuration lives in a single `mkdocs.yml` file. The site name, navigation etc. are defined in a single place.
* **Theme**: The template uses the popular [Material for MkDocs](https://squidfunk.github.io/mkdocs-material/) which supports responsive design with search, dark mode, and more.

```{r echo=FALSE, fig.align='center', out.width='60%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/mkdocs1.png')
```


## 3.1 Updating the Site Configuration 

MkDocs uses a central configuration file named `mkdocs.yml`. This file is in the YAML format (`.yml`). You can learn about all available settings  at [MkDocs Configuration](https://www.mkdocs.org/user-guide/configuration/) page. We will edit this file with the main configuration settings now.

1. From your GitHub repository home, locate the `mkdocs.yml` file. Click on the filename to view it.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/config1.png')
```

2. At the top-right corner, locate the *Edit this file* button and click it.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/config2.png')
```

3. Update the placeholders with your name and information. At the least, you should update the `site_name`, `site_description`, `site_author` and `site_url` values. Once done, click *Commit changes...*. You will be shown a confirmation dialog, click *Comment changes* button again to save the changes.

```{r echo=FALSE, fig.align='center', out.width='70%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/config3.png')
```


# 4. Updating the Site Content

## 4.1 Updating the Homepage

Next, we will now start modifying the content pages of the website. All the content pages as in the Markdown format (.md). Each time you update the markdown file, MkDocs will build and create a new HTML file from that content. We will now update the homepage of the site which is built from the `index.md` file.

1. Locate the `index.md` file in the `docs/` folder. Click to open it and click "Edit this file* button.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit1.png')
```

2. In the file editor, update the placeholders `[YOUR NAME]`, `[YOUR JOB TITLE]`, `[YOUR TAGLINE]` with your information. In the *About Me* section, replace the paragraph with your bio. 

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit2.png')
```

3. Scroll down and update the *Skills* and *Connect* sections. You can delete cards and links which are not relevant for you. Once done, save your changes by clicking *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit3.png')
```

4. The homepage has placeholders for your profile picture and an about me picture. We will now replace these with your pictures. Go to the `docs/assets/images/` folder and click the *...* menu to locate the *Upload files* button.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit4.png')
```

5. Upload your profile photo. Make sure it is named `profile.png` (case sensitive) and is no larger than 300px wide. Wait for the file to be uploaded and click *Commit changes*. Similarly, upload your about me picture named `about.png`. This photo can be any of dimensions, but to ensure the homepage loads fast, ensure it is not bigger than 700px wide. 

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit5.png')
```

6. The homepage also has provision to download your CV. Go to the `docs/assets/` folder and click on the *Upload files* button.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit6.png')
```

7. Upload your CV file named `<your name>-CV.pdf` (If your name is John, name the file `John-CV.pdf`).

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit7.png')
```

8. Your homepage is now ready. Head over to the published website and refresh the tab. You should see the homepage with your pictures and information. If you see any formatting errors, you can go back and update the `index.md` file. If the images do not appear, ensure they are uploaded in the correct folder and the names match as specified in the `index.md` file. 

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/edit8.png')
```

## 4.2 Updating Content Pages (LLM Assisted)

You are now familiar with the process of updating the content of a page with Markdown text. You can update the `contact.md`, `experience.md` and `publications.md` with personalized content. Most of this content will already be present in your CV, LinkedIN profile or Google Scholar. In this section, we will learn how we can use LLM AI Assistants to automatically format existing content to Markdown for easily updating your portfolio. 

> We recommend using [Claude AI](https://claude.ai/new) but you may use your favorite AI assistant.

1. Locate and open the `experience.md` file in the `docs/` folder. This file is for the page on your portfolio that lists your work experience, education, certifications etc. We will automatically create this content from your CV. Copy the GitHub URL for this page.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/llm1.png')
```

2. Go to [Claude AI](https://claude.ai/new) and start a new chat. Attach your CV and enter a prompt like below. Make sure to replace the path to the `experience.md` with the Github URL you copied in the previous step.

```
I want to use the information from my attached CV to update my online portfolio. See the file https://github.com/<your-username>/<your-username>.github.io/blob/main/docs/experience.md for the required format. Format the content from my CV as Markdown that I can copy/paste to update that file.
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/llm2.png')
```

3. Copy the LLM generated Markdown content. Go to the GitHub repository and open the `experience.md` page for editing. Paste the content after the checklist. Preview the output to verify it looks correct and make any adjustments you like. Click *Commit changes...* to save the changes.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/llm3.png')
```

4. Once the site rebuilds, refresh the portfolio and you will see the updated page.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/llm4.png')
```

You can repeat this process for the `publication.md` page to pull your publications from your CV, ResearchGate or Google Scholar profile.

## 4.3 Adding a New Page

The template provides you with a handful of pages pre-configured for the website. It is quite easy to add new pages. Adding a new page is a 2-step process - you first create a new file with the content and then update the `mkdocs.yml` file to add it to the navigation. Let's create a new page.

1. Go to the `docs` folder and click **Add file &rarr; + Create new file**.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/addpage1.png')
```

2. You can name the page with the `.md` extension. Here we are creating a new page that lists learning resources so we have named it `resources.md`. Add the content for the page in the Markdown format. Here we are using the [Grids](https://squidfunk.github.io/mkdocs-material/reference/grids/) provided by the Material theme to organize the resources in a responsive layout. Click *Commit changes...* to save the file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/addpage2.png')
```

3. Next go to the root of your repository and locate the `mkdocs.yml` file. Open it for editing and scroll down to the `nav:` section. Add the newly created page to the navigation menu. Click *Commit changes...* to save the file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/addpage3.png')
```

4. Once the site rebuilds, refresh the portfolio and you will see the newly added page.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/addpage4.png')
```

## 4.4 Deleting a Page

If there are pages from the template that you would like to remove, you can forst remove it from the navigation menu in `mkdocs.yml` and delete the file. Let's see the process by removing the `publications.md` page.

1. Open the `mkdocs.yml` file and scroll down to the `nav:` section. Locate the item with the page that you would like to delete.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete1.png')
```

2. Delete the line and click *Commit changes...*. This will now remove the reference to this page from the navigation menu.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete2.png')
```

3. Go to the `docs/` folder and open the file that you want to delete.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete3.png')
```

4. Click on the *...* menu and select *Delete file*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete4.png')
```

5. Click *Commit changes...* to delete the file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete5.png')
```

6. Once the site rebuilds, refresh the portfolio and you will see the updated menu.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/delete6.png')
```

## 4.5 Adding a Project Page (LLM Assisted)

The portfolio template is custom design to showcase your project. You can add multiple projects to your site. Each project will be shown using a card layout. Adding a project requires uploading a cover image and creating a Markdown file.

1. Go to the `docs/assets/images/` folder and click **Add file &rarr; Upload files**.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project1.png')
```

2. Upload a cover image for your project and click *Commit changes...*. The project images are shown as horizonal cards, so choose an appropriate image with a landscape layout.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project2.png')
```

3. The template comes with a `sample-project.md` file with suggested sections for a project. Open the file and click on the *Raw* button. Copy the URL. We will use this to prompt an LLM Assistant to help us draft the project page.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project3.png')
```

4. Go to [Claude AI](https://claude.ai/new) and start a new chat. Enter a prompt shown below, replacing the image name and file path with your own. Claude will ask you questions about the project and share as much information as you can. You can optionally attach your project report or add a link with information about your project. Once the content is created, click the *Copy* button.

```
I want to add a project to my portfolio
My image is uploaded at docs/assets/images/<image-path>.png

My template is at https://raw.githubusercontent.com/<yourusername>/<your-username>.github.io/refs/heads/main/docs/projects/sample-project.md 

Ask me questions regarding my project and once you have enough information, generate the content in Markdown format that I can use to create the page
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project4.png')
```

5. On GitHub, go to `docs/projects/` folder and create a new file. Enter the name as `<project-name>.md` and paste the content. Edit the content as required and click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project5.png')
```

6. We need to add the project to the card layout. Open the `docs/projects/index.md` file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project6.png')
```

7. Edit the file with the project name and description. Update the cover image and project file path. Click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project7.png')
```

8. Next, we add this project to the navigation menu. Edit the `mkdocs.yml` file and add the name and path of the project file and click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project8.png')
```

9. Once the site rebuilds, refresh the portfolio and head over to the *Projects* page to see the new project.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/project9.png')
```

## 4.6 Adding a Notebook Project

The portfolio template also supports using Jupyter Notebooks (`.ipynb`) as project pages. The notebooks can have cells with Markdown content, code, charts, interactive maps. These will be converted to compatible Markdown content by MkDocs and will be rendered as a project page. Adding a project via a notebook requires uploading a cover image and a notebook file.

1. Prepare your project notebook. Make sure to add a Markdown cell at the top with the project title. You may clear outputs of cells with warnings or unwanted outputs so your resulting page is clean. Also make sure to replace any API Keys with placeholders.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook1.png')
```

2. On Github, go to the `docs/projects/` folder and click *Upload files*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook2.png')
```

3. Select the project notebook file and click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook3.png')
```

4. Next, go to the `docs/assets/images/` folder and upload the cover image for the project.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook4.png')
```

5. Now we will update the `index.md` fil in the `docs/projects/` folder with the card for the newly uploaded project.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook5.png')
```

6. Replace the placeholder project card with the details of your notebook project.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook6.png')
```

7. Next, we add this project to the navigation menu. Edit the `mkdocs.yml` file and add the name and path of the project file and click *Commit changes...*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook7.png')
```

8. Once the site rebuilds, refresh the portfolio and head over to the *Projects* page to see the new project.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/notebook8.png')
```

# 5. Using a Local Development Environment

Local development environment lets you preview changes instantly before pushing to GitHub. In this sectino, we will clone your GitHub repository to the computer, install the required packages, start a local preview server, edit the files using a code editor, preview the updated portfolio and then push the changes to GitHub.

1. In your GitHub repository, click the *Code* button and copy the repository URL shown for *HTTPS*. The URL should be in the form `https://github.com/<your-username>/<your-username>.github.io.git`.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local2.png')
```

2. *(Windows users)* Once installed, search for Anaconda Prompt in the Start Menu launch it. *(Mac/Linux users)*: Launch a Terminal window. Use the `cd` (Change Directory) command to switch the local directory to `Desktop` or any other preferred folder. Then type the following command to clone the repository - replacing the URL with your own repository URL.

```bash
cd Desktop
```

```bash
git clone https://github.com/<your-username>/<your-username>.github.io.git
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local3.png')
```

3. This will copy the repository to your computer. Use the `cd` command to go to the folder with the newly cloned repository.

```bash
cd <your-username>.github.io
```

4. The repository contains a [`environment.yml`](https://docs.conda.io/projects/conda/en/latest/user-guide/tasks/manage-environments.html#creating-an-environment-from-an-environment-yml-file) file with instructions for a conda environment. Run the following command to create a new environment using this file. This will create a new conda environment named `portfolio` and install the required packages.

```bash
conda env create -f environment.yml
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local4.png')
```

5. Once the installation finishes, activate the environment.

```bash
conda activate portfolio
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local5.png')
```

6. Next, we start a local MkDocs server which will build the portfolio website from the source content. The `--livereload` option tells the server to watch the files in the repository and automatically rebuild the site whenever we update any content.

> A warning maybe displayed about the MkDocs 2.0 version. You can ignore it.

```bash
mkdocs serve --livereload
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local6.png')
```

7. Once the server has started, open your browser and go to [http://127.0.0.1:8000](http://127.0.0.1:8000). You will see the portfolio website.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local7.png')
```

8. We can now edit the content and immediately see its effect on the site. In the code editor, click the *Open Folder* button and browse to the local repository folder `Desktop\<your-username>.github.io`.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local8.png')
```

9. If you are prompted with a security warning, click *Yes, I truest the authors.*.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local9.png')
```

10. Let's change the theme color and preview it. Open the `mkdocs.yml` file and change the `theme.palette.primary` color to any of the available colors. Save the file.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local10.png')
```

11. Switch to your browser and you should see the color applied instantly.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local11.png')
```

12. Let's make this change in the repository and push it to our live portfolio. In the *Anaconda Prompt* / *Terminal* window, press **Ctrl+C** to stop the preview server. Once the prompt returns, type the following command.

```bash
git status
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local12.png')
```

13. It will show that the `mkdocs.yml` file has been edited. Run the following command to commit it.

```bash
git commit -a -m "Update Color"
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local13.png')
```

14. Next, we push these changes to GitHub. Run the following command.

```bash
git push origin main
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local14.png')
```

15. You will be prompted to sign-in to your GitHub account. Click *Sign in with your browser* button and complete the authentication flow.

```{r echo=FALSE, fig.align='center', out.width='50%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local15.png')
```

16. Once authentication is done, your changes will be pushed to the remote GitHub repository.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local16.png')
```

17. Wait for the GitHub pages to build and open the portfolio URL to see the changes applied to the published website.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/local17.png')
```

# Supplement

## Adding a Cover Image on the Homepage

To add a cover image to your homepage, add a banner image `assets/images/hero-image.png` and update the *Hero section* in the `assets/css/extra.css` as below.

```css
/* Hero section on the Home page */
.hero {
  text-align: center;
  padding: 1rem 1rem 1.25rem 1rem;
  background:
    linear-gradient(135deg, 
      rgba(232,244,248,0.82) 0%,
      rgba(240,247,255,0.82) 50%, 
      rgba(232,240,254,0.82) 100%),
    url('../images/hero-image.png') center / cover no-repeat;
  border-radius: 8px;
}
```

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/hero_cover.png')
```

## Using a Custom Domain

GitHub Pages allows to connect your site to your own domain name and host the GitHub Pages site on it. See [About custom domains and GitHub Pages
](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/about-custom-domains-and-github-pages) for official instructions. We are providing simplified version of these instructions here for the most common setup.

1. To use a custom domain, you need to purchase a domain name. There are many registrars who sell domain names. The cost depends on the uniqueness of the domain and the extension (like `.com`, `.in`, `.ai`). We recommend [Hover](https://www.hover.com/) for purchasing the domain but you can use any registrar of your choice. All you need is the access to the control panel.

> Make sure to include [WHOIS privacy](https://support.hover.com/support/solutions/articles/201000064738-domain-whois-privacy) when purchasing a domain. Skipping this may expose your contact information to the public.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/domain1.png')
```

2. Once you have access to the domain, login to the control panel. Go to *Forwards* and setup a forward from `your-domain.com` to `www.your-domain.com`. This greatly simplifies the configuration needed to connect the domain to GitHub.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/domain2.png')
```

3. Next, go to the *DNS* and add a `CNAME` record for the host `www` with the value `your-username.github.io` (replacing `your-username` with your GitHub username).

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/domain3.png')
```

4. Now, go to your GitHub repository. Go to **Settings &rarr; Pages** and add the custom domain name. Make sure to prefix `www` to the name and click *Save*. The site URL will update and your portfolio will be visible at the new domain.

> DNS propagation can take some time. So you may need to wait 15-30mins before the new domain starts working.

```{r echo=FALSE, fig.align='center', out.width='75%'}
knitr::include_graphics('images/geospatial_portfolio_workshop/domain4.png')
```

# License

This workshop material is licensed under a [Creative Commons Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/). You are free to re-use and adapt the material but are required to give appropriate credit to the original author as below:

*Building Your Geospatial Portfolio Website* by Ujaval Gandhi [www.spatialthoughts.com](https://spatialthoughts.com)

&copy; 2026 Spatial Thoughts [www.spatialthoughts.com](https://spatialthoughts.com)