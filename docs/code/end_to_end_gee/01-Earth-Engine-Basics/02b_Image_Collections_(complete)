var s2 = ee.ImageCollection("COPERNICUS/S2");
// Before you do anything with the collection,
// it is useful to see information about a single image
// contained in the collection. The .first() function
// prints the information about the first image in the 
// collection
print(s2.first())

// Visualizing the collection
// To see the collection on a map, we need to use Map.addLayer() function
// But before that we need to know some parameters
// The Earth Engine data catalog provides suggested visualization parameters
// for all collections. 
// See https://developers.google.com/earth-engine/datasets/catalog/COPERNICUS_S2
// See 'Explore in Earth Engine' section 
var rgbVis = {
  min: 0.0,
  max: 3000,
  bands: ['B4', 'B3', 'B2'],
};
// Now we can use these to add the collection to the map
Map.addLayer (s2, rgbVis, 'Sentinel-2 RGB')
