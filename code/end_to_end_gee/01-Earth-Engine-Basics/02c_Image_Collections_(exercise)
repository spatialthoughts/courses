var s2 = ee.ImageCollection("COPERNICUS/S2");
var rgbVis = {
  min: 0.0,
  max: 3000,
  bands: ['B4', 'B3', 'B2'],
};
Map.addLayer(s2, rgbVis, 'Sentinel-2 RGB')

// Add another layer which shows a False-Color-Infrated Composite
// Hint: False Color Infrared is created from NIR, Red, Green bands
